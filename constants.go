package pkg

const (
	// Github defines the github provider based on the name passed to the command.
	Github = "github"
	// Gitlab defines the gitlab provider based on the name passed to the command.
	Gitlab = "gitlab"
)
